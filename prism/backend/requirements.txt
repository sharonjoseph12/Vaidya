# PRISM Platform - Backend Dependencies
# Person 4: Platform Engineer
#
# Install: pip install -r prism/backend/requirements.txt
# For real multimodal SENSE + finetuned stack, also install:
#   pip install -r prism/backend/requirements-ml.txt
# (Use Python 3.11–3.12 if your platform lacks wheels for 3.14.)

# Web Framework (newer pins ship wheels for Python 3.14 / Windows without MSVC)
fastapi>=0.115.6,<0.117
uvicorn[standard]>=0.32.0,<0.36
python-multipart>=0.0.9,<0.1

# Database (pin avoids heavy ``pyiceberg`` / MSVC build pulled by newer storage clients)
supabase==2.3.4

# Async Task Queue
celery>=5.3.6,<6
redis>=5.0.1,<6

# HTTP Client
httpx>=0.24.0,<0.28.0

# Authentication
python-jose[cryptography]>=3.3.0,<4

# Encryption
cryptography>=41.0.2,<44

# Data Validation
pydantic>=2.10.6,<3
pydantic-settings>=2.6.0,<3

# FHIR / ABDM
fhir.resources>=7.1.0,<8

# Federated Learning (optional — install ``flwr`` via ``requirements-ml.txt``)

# PDF Generation
reportlab>=4.1.0,<5

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.6
pytest-cov>=4.1.0

# Utilities
python-dotenv>=1.0.0,<2
filelock>=3.13.0,<4
