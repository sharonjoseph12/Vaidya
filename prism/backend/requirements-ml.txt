# Optional ML stack: Layer 1 SENSE, PyTorch artifacts, audio/video deps.
# Prefer Python 3.11–3.12 on Windows for prebuilt wheels.
#
#   pip install -r prism/backend/requirements-ml.txt
#
# Optional Keras finetuned head (``prism_yamnet_audio.h5``); otherwise use
# ``python scripts/bootstrap_presentation_artifacts.py`` which writes ``prism_yamnet_torch.pt``.
# tensorflow>=2.15.0

numpy>=1.26.0,<2.1
torch>=2.3.0
librosa>=0.10.0
opencv-python-headless>=4.9.0
PyYAML>=6.0
mediapipe>=0.10.0
joblib>=1.3.0
scikit-learn>=1.3.0

# Flower FL (optional; depends on numpy — install with ML stack)
flwr==1.6.0
